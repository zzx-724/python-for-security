# 🐍 Python 学习方案 — 密码科学与技术 / 网安方向

> 制定日期：2026-09-10
> 适用对象：准大二、密码科学专业、C 语言刚学完语法、每天 1-2h、项目驱动型

---

## 一、你的个人画像

| 维度 | 情况 |
|------|------|
| 专业 | 密码科学与技术（准大二） |
| C 语言基础 | 学完语法，懂基本控制流和函数，指针/数据结构尚未深入 |
| Python 起点 | 写过几行，基本算零基础 |
| 核心目标 | 网安渗透脚本 > 密码学编程 > 爬虫与自动化 |
| 可用时间 | 每天 1-2 小时 |
| 学习方式 | 边做边学，项目驱动，不喜欢干看理论 |
| 就业窗口 | 还有 3 年，时间充裕但要方向对 |

## 二、为什么 Python 是你的必修课

在你这个赛道，Python 几乎是"标配工具"：

- **CTF/渗透测试**：信息收集脚本、EXP 编写、漏洞验证、自动化攻击链
- **密码学**：算法原型验证、加解密工具、数学运算（比 C 快 10 倍写出原型）
- **安全研究**：流量分析、日志解析、恶意代码分析辅助、Fuzzing
- **面试竞争力**：安全岗位 JD 普遍要求"熟练 Python"

**你的独特优势**：密码科学专业 + 能写脚本 = 同时懂理论和工程的人很少，很值钱。

---

## 三、三阶段学习路线

```
阶段一（2周）         阶段二（4周）             阶段三（长期）
Python 语法速通      安全方向 Python 工具箱     实战项目积累
───────────────── → ───────────────────── → ─────────────────
基础 + 密码学小练习   网络/Web/系统/加密       端口扫描器/爆破器
                                                  /CTF自动化/WAF绕过
```

### 🔹 阶段一：Python 语法速通（第 1-2 周）

**核心理念**：不追求面面俱到，**只学安全方向常用的 20% Python 特性**，剩下 80% 用到再查。

| 天 | 学什么 | 为什么安全方向需要 | 你做的小项目 |
|----|--------|-------------------|-------------|
| 1 | 变量、类型、print/input、f-string | 输出扫描结果、格式化报告 | 交互式信息收集问卷 |
| 2 | 字符串操作（切片/方法/格式化） | 处理 URL、Payload、Base64、日志 | 写一个简单的 Payload 生成器 |
| 3 | 列表/字典/元组/集合 | 存端口列表、服务字典、去重 IP | IP 段解析 + 端口字典生成 |
| 4 | 条件/循环/推导式 | 遍历扫描结果、过滤存活主机 | 网段存活主机探测模拟 |
| 5 | 函数/参数类型/异常处理 | 封装扫描模块、容错 | 把之前的代码模块化 |
| 6 | 文件读写 + JSON/CSV | 读目标列表、写扫描报告 | 批量 URL 读取 + 结果导出 JSON |
| 7 | 模块/包/pip | 用第三方库而不是自己造轮子 | 安装 requests，拿网页状态码 |
| 8 | 面向对象入门 | 抽象扫描器、漏洞、目标等实体 | Scanner 类封装一个简单扫描 |
| 9 | 阶段一小项目 | 综合练习 | **简易信息收集工具**：读 URL 列表 → 发 HTTP 请求 → 提取标题/状态码 → 导出报告 |

> ⚠️ **阶段一的目标不是"学完 Python"，而是"能用 Python 写出一个安全相关的脚本"。**

---

### 🔹 阶段二：安全方向 Python 工具箱（第 3-6 周）

每周聚焦一个安全相关的 Python 能力模块，每个模块有一个输出项目。

#### 第 3 周：网络编程基础
- `socket` 模块：TCP/UDP 连接、发送接收数据
- 多线程/并发初步：`threading`，`concurrent.futures`
- **项目**：**多线程端口扫描器**（扫描目标 IP 的 1-65535 端口，识别开放服务和 banner）

#### 第 4 周：HTTP 与 Web 安全
- `requests` 深度使用：Session、Headers、Proxy、Cookie、HTTPS
- `urllib`/`BeautifulSoup`：解析 HTML
- 正则表达式 `re`：从响应中提取信息
- **项目**：**目录/文件爆破器**（遍历字典，发现隐藏路径，判断 200/403/404）

#### 第 5 周：密码学 Python 实践
- `hashlib`：MD5/SHA1/SHA256，为什么 MD5 不安全（实操演示）
- `base64`/`binascii`：编码/解码
- `Crypto`（pycryptodome）：AES/RSA 加解密
- **项目**：**加解密工具箱**（Base64 编解码、哈希计算、简单 AES 加解密、CTF 常见编码转换）

#### 第 6 周：系统安全与自动化
- `subprocess`：调用外部安全工具（nmap、sqlmap 等），管道交互
- `os`/`sys`/`argparse`：命令行参数、路径操作
- **项目**：**自动化扫描调度器**（接收目标，按顺序调用多个工具，收集输出，生成报告）

---

### 🔹 阶段三：实战项目积累（长期，持续到毕业）

每完成一个项目就放进 GitHub，这是你校招时最好的简历。

#### 必做项目（有难度梯度）

| 序号 | 项目 | 难度 | 涉及技能 | 预估行数 |
|------|------|------|---------|---------|
| 1 | 子域名爆破器 | ⭐ | requests, 多线程, DNS | ~100 行 |
| 2 | SQL 注入检测脚本（基于报错/时间盲注） | ⭐⭐ | requests, re, 时间处理 | ~200 行 |
| 3 | Fuzz 模糊测试工具（目录/参数/后缀） | ⭐⭐ | 文件读写, 多线程, re | ~150 行 |
| 4 | 简单 WAF 绕过 Payload 生成器 | ⭐⭐⭐ | 字符串变换, 编码组合 | ~200 行 |
| 5 | XSS Payload 测试框架 | ⭐⭐⭐ | requests, selenium, re | ~300 行 |
| 6 | CTF 密码学解题脚本集（持续积累） | ⭐⭐~⭐⭐⭐⭐ | 数论, 加解密库 | 不定 |

#### CTF 平台推荐（用 Python 去打）

- **BUUCTF**（国内，新手友好）
- **AttackDefense**（实战场景）
- **CTFlearn**（国际，题目分级清晰）

---

## 四、推荐资源（安全导向筛选）

### 必读/必看

| 资源 | 类型 | 说明 |
|------|------|------|
| 《Python 编程：从入门到实践》 | 书 | 只看前半本语法，后半本三个项目你选 Web 那个做 |
| 《Python 黑帽子：黑客与渗透测试编程之道》 | 书 | **阶段二再读**，每章一个攻击工具实战，跟你的目标完美匹配 |
| 《Python 绝技：运用 Python 成为顶级黑客》 | 书 | 和黑帽子互补，更偏实用技巧 |
| 廖雪峰 Python 教程 | 网页 | 当作语法速查手册，不要从头看到尾 |

### 安全方向必会库清单

```
阶段一：内置模块（无需 pip）
├── 字符串方法（split, join, strip, encode/decode）
├── json, csv（报告格式）
├── os, sys, pathlib（文件系统操作）

阶段二：安全核心库
├── requests      ← HTTP 交互（最重要）
├── socket         ← 网络编程
├── re             ← 正则匹配（提取关键信息）
├── hashlib        ← 哈希计算
├── base64         ← 编解码
├── pycryptodome   ← AES/RSA/DES 加解密
├── threading      ← 并发提速
├── argparse       ← 命令行工具

阶段三：进阶
├── scapy          ← 网络数据包操作
├── paramiko       ← SSH 连接
├── BeautifulSoup  ← HTML 解析
├── selenium       ← 浏览器自动化
├── pwntools       ← CTF PWN 题神器
└── mitmproxy      ← HTTP 流量拦截
```

---

## 五、学习节奏建议

### 每天的 1-2 小时怎么分配

```
前 30 分钟：看新知识（教程/代码注释）
             ↓
后 45 分钟：动手写代码（敲一遍 + 改一改测试）
             ↓
最后 15 分钟：复盘 + 记笔记（今天学了什么，遇到了什么坑）
```

### 每周节奏

```
周一～五：按计划推进新知识（每天 1 个小练习）
周六：     用本周学的内容做一个完整小项目
周日：     休息 or 打 CTF 娱乐
```

### 笔记建议

在 GitHub 开一个仓库 `python-for-security`，每个练习和项目都传上去：
- 既是学习记录，又是校招简历
- 每个项目写清楚 README：功能、用法、截图
- Commit 记录展示你的学习轨迹

---

## 六、关键思维转换（C → Python）

### 写法对比（安全场景）

```python
# 场景：读取目标列表，发请求，收集结果

# ❌ C 风格（别这样写）
urls = ["http://a.com", "http://b.com", "http://c.com"]
for i in range(len(urls)):           # 别用 range(len())
    url = urls[i]
    print(url)

# ✅ Python 风格
urls = ["http://a.com", "http://b.com", "http://c.com"]
for url in urls:                      # 直接遍历
    print(url)

# ❌ C 风格：手动拼接 Payload
payload = "' OR '1'='1"
sql = "SELECT * FROM users WHERE name = '" + payload + "'"

# ✅ Python 风格：用 f-string
payload = "' OR '1'='1"
sql = f"SELECT * FROM users WHERE name = '{payload}'"

# ❌ C 风格：手动 fopen/fclose
f = open("targets.txt", "r")
lines = f.readlines()
f.close()                            # 容易忘

# ✅ Python 风格：with 自动管理
with open("targets.txt", "r") as f:
    lines = f.readlines()
# 自动关闭，不会忘
```

---

## 七、学习进度追踪

> 开始日期：2026-09-10
> 当前阶段：阶段一 · Day 4 已完成，准备进入 Day 5

### 已完成 ✅

| 日期 | 内容 | 掌握情况 |
|------|------|---------|
| 09-10 | 环境配置 | Python 3.14.5 安装成功，VS Code 配置完毕 |
| 09-10 | 运行方式 | 学会 Ctrl+F5 在终端运行（Code Runner 不适合 input） |
| 09-11 | Day 1 完成：条件判断 | if/elif/else 掌握，端口服务识别 + 风险评估练习 ✅ |
| 09-14 | Day 2 完成：字符串操作 | 切片/find/split/join/replace/strip/编解码全部掌握，完成 URL 解析 + Payload 生成 3 个练习 ✅ |
| 09-16 | Day 3 完成：列表/字典/集合 | 四种容器全掌握，3 个练习全通过（含去重扫描结果合并）✅ |
| 09-18 | Day 4 完成：循环与推导式 | for/range/while/enumerate/zip/推导式全掌握，4 个练习通过 ✅ |
| 09-18 | 学会 git | `add`→`commit`→`push` 流程独立完成，理解暂存区与"分批提交" |

### Day 1 进度：变量、类型、print/input

| 知识点 | 状态 | 笔记 |
|--------|:--:|------|
| `print()` 基本用法 | ✅ | 掌握 |
| `print()` 逗号自动加空格 | ✅ | **坑**：`print("a=",a)` → `a= 1`，用 f-string 解决 |
| f-string 格式化 | ✅ | `f"a={a}"` 无多余空格，推荐使用 |
| `input()` | ✅ | 永远返回字符串！需要 `int()`/`float()` 转换 |
| `int()` 类型转换 | ✅ | 相当于 C 的 `atoi()` |
| 变量赋值 | ✅ | 无需声明类型，动态类型 |
| `if/elif/else` 条件判断 | ✅ | 掌握三分支结构，注意冒号和缩进 |
| `==` vs `=` | ✅ | `==` 比较，`=` 赋值，不搞混 |
| `in` 运算符 | ✅ | `port in [80, 443]` 比一堆 `or` 简洁 |
| 端口服务识别器 | ✅ | practice_01.py 完成，能根据端口判断服务和风险等级 |

### Day 2 进度：字符串操作

| 知识点 | 状态 | 笔记 |
|--------|:--:|------|
| 字符串切片 `[start:end]` | ✅ | 含头不含尾！`url[6:17]` 取索引6~16 |
| 切片省略写法 | ✅ | `s[12:]` = 到结尾，`s[:5]` = 从开头 |
| `index()` 查找位置 | ✅ | 返回首次出现位置，可指定起始搜索位置 |
| `find()` vs `index()` | ✅ | `find` 找不到返回 -1 不崩，安全脚本优先用 `find` |
| URL 域名提取 | ✅ | 找 `//` 定位 start → 找 `/` 定位 end → 切片 |
| `split()` | ✅ | **坑**：开头/结尾是分隔符会产生空串 `''`（空串≠空格） |
| `join()` | ✅ | `split` 的逆操作，列表开头的空串会拼出开头的 `/` |
| `replace()` | ✅ | 模板 + 占位符 = Payload 生成器核心，可链式调用 |
| `strip()` | ✅ | **坑**：`"200 OK\n" != "200 OK"`，比较前必须 strip |
| `upper()`/`lower()` | ✅ | WAF 大小写绕过基础 |
| `startswith()`/`endswith()` | ✅ | 判断 URL 特征、文件后缀 |
| base64 / hex 编解码 | ✅ | `b64encode`/`b64decode`、`encode().hex()` |
| **读懂报错** | ✅ | SyntaxError(编译期，整个程序不跑) vs 运行期错误 |

### Day 2 踩过的坑（重要）

1. **`if` 行末尾漏冒号** → `SyntaxError: expected ':'`，整个文件一行都不执行
2. **`{}` 用错场合** → 花括号只在 `f"..."` 字符串里才是插值，写在普通代码里是"集合"
3. **变量重复赋值** → 同名变量赋值 3 次只剩最后一个，要写一次 print 一次
4. **`.upper` 漏括号** → 拿到的是"方法本身"不是字符串
5. **`sql[12;len(sql)]`** → 切片用冒号 `:` 不是分号 `;`

### Day 3 进度：列表、字典、集合

| 知识点 | 状态 | 笔记 |
|--------|:--:|------|
| 列表创建/索引/切片 | ✅ | 切片规则和字符串完全一致，`[-1]` 取最后一个 |
| 列表增删改 | ✅ | `append` 末尾加、`insert` 插位置、`remove` 按**值**删、`pop` 按**位置**删并返回值 |
| `in` 判断 | ✅ | `3306 in ports` → True/False |
| 列表相加/相乘 | ✅ | `[1,2]+[3,4]` 拼接，`[0]*3` 重复 |
| 元组 tuple | ✅ | `()` 创建，**不可修改**，用于表示固定数据 |
| 字典 dict | ✅ | `{}` 键值对，按 key 查值 |
| `.get()` 安全取值 | ✅ | 第二个参数是**默认值**；不写返回 `None`；`[]` 取不到会 KeyError 崩溃 |
| 字典增/改/删 | ✅ | 直接赋值即可（key 存在=改，不存在=增），`del` 删除 |
| 集合 set | ✅ | 自动去重，`\|` 并集、`&` 交集、`-` 差集，**无序** |
| 多源结果合并 | ✅ | 交集=高置信度，差集=待复核疑点（真实渗透工作流） |

### Day 3 踩过的坑（重要）

1. **`remove(值)` vs `pop(位置)`** — 前者按值删，后者按位置删并返回
2. **`dict[key]` 查不到会崩溃** — 扫描脚本必须用 `.get(key, 默认值)`
3. **`in` 判断字典时只判断 key**，不判断 value
4. **集合无序** — 打印顺序可能每次不同，需要有序就 `sorted()`
5. **f-string 嵌套引号** — `f"{d.get(x,"y")}"` 只有 Python 3.12+ 能跑，**一律用「外双内单」**

### Day 4 进度：循环与推导式

| 知识点 | 状态 | 笔记 |
|--------|:--:|------|
| `for` 循环 | ✅ | 直接遍历元素，不用管索引（对比 C 的 `i`） |
| `range()` | ✅ | **含头不含尾**！`range(1,11)` → 1~10，对应 C 的 `i <= 10` |
| `while` 循环 | ✅ | 不知道循环几次时用；**别忘递增变量，否则死循环** |
| `break` / `while...else` | ✅ | `else` 只在"没被 break"时执行（冷门语法） |
| `enumerate()` | ✅ | 产出 `(序号, 元素)` 元组，**第一个变量接序号** |
| `zip()` | ✅ | 配对遍历，**顺序 = 传参顺序**；长度不同以短的为准 |
| 嵌套解包 | ✅ | `for i, (a, b) in enumerate(zip(X, Y), 1)` —— 变量形状要匹配数据形状 |
| 嵌套循环 | ✅ | 外层 1 次 = 内层转满一轮（2×3=6 行） |
| 列表推导式 | ✅ | `[p for p in lst if 条件]`，读法：从中间往两边 |
| 字典推导式 | ✅ | `{k: v for k in lst}` |
| 嵌套推导式 | ✅ | 两个 for，顺序和嵌套循环一致 |
| 格式说明符 | ✅ | `{port:6d}` 宽度右对齐、`{ip:<15}` 左对齐、`{rate:.2f}` 小数 |

### Day 4 踩过的坑（重要）

1. **`==` vs `in`** — `port == (80,443)` 永远 False（一个数字不等于一个元组）；要判断"是其中之一"必须用 `in`。**这个 bug 不报错，只是静默少输出**
2. **`range(1, 11)` 不是 `range(1, 10)`** — 含头不含尾，写成 10 会少扫一个 IP
3. **enumerate/zip 变量写反不报错** — Python 只看位置不看名字，名字写反了程序照跑但数据全错
4. **`commit` ≠ `push`** — commit 只存本地，忘了 push 时 `git status` 会显示 `[ahead 1]`
5. **嵌套解包要加括号** — `for i, (a, b) in ...`，不加会 `ValueError: not enough values to unpack`

### 已完成：GitHub 仓库 ✅

- [x] GitHub 仓库创建：https://github.com/zzx-724/python-for-security
- [x] 首次提交并推送（11 个文件，1849 行）

**日常更新流程**（每次学完跑一遍）：

```bash
git add .
git commit -m "Day 4: 循环与列表推导式"
git push
```

### 待完成 ⬜

- [ ] 做完 `practice_02_enumerate_zip.py`（7 道专项练习 + 挑战题）
- [ ] Day 5：函数 / 参数 / 异常处理（`06_crypto_basics.py` 之前先补函数）

---

> 📝 进度：2026-09-18 Day 4 已完成。下次：先做 `practice_02_enumerate_zip.py` 的练习，然后开始 Day 5 函数

---

## 八、常见的坑 & FAQ

**Q: 要不要学算法/数据结构再学 Python？**
A: 不需要。安全脚本重点是"快速实现想法"，先写出来，跑得慢再优化。LeetCode 算法题和安全脚本是两条线。

**Q: Python2 还是 Python3？**
A: Python3。Python2 已停止维护，安全工具也基本全部迁移了。某些老工具（如 sqlmap）还兼容 Py2，但你的新脚本全用 Py3 写。

**Q: 要不要装 Kali Linux？**
A: 暂时不用。Windows + Python + VS Code 足够起步。阶段二做网络编程时可以考虑 WSL 或虚拟机装 Kali。

**Q: 每天 1-2 小时够不够？**
A: 够了。安全这条路是马拉松不是百米冲刺，持续 3 年每天 1 小时 >> 暑假突击 2 周每天 8 小时然后放弃。

---

> 💡 **给你的关键建议**：你是密码科学专业的，这个专业最大的优势就是"懂数学+能编程"的复合能力。Python 是你把数学公式变成攻击/防御工具的桥梁。阶段一尽快过掉基础语法，快速进入阶段二的工具编写——那是你真正上瘾的地方。
